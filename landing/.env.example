# API Configuration
# Prefer 127.0.0.1 over localhost on Windows (localhost → ::1 can hit Docker on :4000).
# In production, this will use the default production URL
VITE_API_URL=http://127.0.0.1:4000
