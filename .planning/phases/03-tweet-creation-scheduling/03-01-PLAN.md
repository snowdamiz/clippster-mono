---
phase: 03-tweet-creation-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/clippster_server/social/platforms/twitter.ex
  - server/lib/clippster_server/social/post_submission.ex
autonomous: true

must_haves:
  truths:
    - "Twitter.create_tweet/3 sends POST to api.x.com/2/tweets with text and media_ids"
    - "Twitter.create_tweet/3 returns {:ok, %{post_id, post_url}} on 201 response"
    - "Twitter.create_tweet/3 constructs post_url from username via get_user_profile fallback to /i/ URL"
    - "PostSubmission validates caption max 280 chars when platform is twitter"
    - "PostSubmission validates caption max 2200 chars when platform is instagram (unchanged)"
  artifacts:
    - path: "server/lib/clippster_server/social/platforms/twitter.ex"
      provides: "create_tweet/3 function for X API v2 POST /2/tweets"
      contains: "def create_tweet"
    - path: "server/lib/clippster_server/social/post_submission.ex"
      provides: "Platform-specific caption length validation"
      contains: "twitter"
  key_links:
    - from: "server/lib/clippster_server/social/platforms/twitter.ex"
      to: "https://api.x.com/2/tweets"
      via: "HTTPoison.post in create_tweet/3"
      pattern: "api\\.x\\.com/2/tweets"
    - from: "server/lib/clippster_server/social/platforms/twitter.ex"
      to: "get_user_profile/1"
      via: "post_url construction after tweet creation"
      pattern: "get_user_profile.*access_token"
    - from: "server/lib/clippster_server/social/post_submission.ex"
      to: "platform field"
      via: "conditional validation in validate_caption"
      pattern: "twitter.*280|280.*twitter"
---

<objective>
Add Twitter.create_tweet/3 function and platform-specific caption validation for X posting.

Purpose: Provides the core tweet creation API call that Phase 3 Plan 02 will wire into the ScheduledPostWorker and PostSubmissionController. Also enforces X's 280-character caption limit at the schema level to prevent scheduling posts that will fail at publish time.

Output: Working create_tweet/3 in Twitter module, platform-aware caption validation in PostSubmission schema.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tweet-creation-scheduling/03-RESEARCH.md
@.planning/phases/02-chunked-video-upload/02-02-SUMMARY.md
@server/lib/clippster_server/social/platforms/twitter.ex
@server/lib/clippster_server/social/post_submission.ex
@server/lib/clippster_server/social/platform.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add create_tweet/3 to Twitter platform module</name>
  <files>server/lib/clippster_server/social/platforms/twitter.ex</files>
  <action>
Add a new public function `create_tweet/3` to the Twitter module, placed between the existing `publish_media/3` and `get_insights/2` functions in the Platform Callbacks section.

Implementation details:
- Module attribute: `@tweets_url "https://api.x.com/2/tweets"`
- Function signature: `def create_tweet(access_token, text, opts \\ [])`
- Build JSON body with `"text"` key, and optionally `"media"` key containing `"media_ids"` list from `opts[:media_ids]`
- Send POST to @tweets_url with Bearer auth header and Content-Type application/json
- On 201 response: parse JSON, extract `data.id` as post_id
- Construct post_url: call `get_user_profile(access_token)` to get username, build `"https://x.com/#{username}/status/#{post_id}"`. On profile fetch failure, fallback to `"https://x.com/i/status/#{post_id}"`
- On non-201 response: extract error using existing `extract_error/2` helper, return `{:error, reason}`
- On HTTP error: return `{:error, reason}`
- Add Logger.info for tweet creation start and success, Logger.error for failures
- Use existing `@http_options` for timeout configuration

Helper functions to add (private):
- `defp build_tweet_body(text, opts)` - builds and JSON-encodes the request body
- `defp handle_tweet_success(response_body, access_token)` - parses response and constructs post_url

Return type: `{:ok, %{post_id: String.t(), post_url: String.t()}}` or `{:error, term()}`

NOTE: This function is NOT a Platform behaviour callback. It's a Twitter-specific function called directly via `Platform.call("twitter", :create_tweet, [...])`. Do NOT add `@impl true` to it.
  </action>
  <verify>
Run `cd /Users/sn0w/Documents/dev/clippster-mono/server && mix compile --warnings-as-errors` - must compile with zero warnings. Grep for `def create_tweet` in twitter.ex to confirm function exists. Grep for `@tweets_url` to confirm module attribute. Grep for `api.x.com/2/tweets` to confirm correct endpoint.
  </verify>
  <done>
Twitter.create_tweet/3 exists, compiles cleanly, targets POST https://api.x.com/2/tweets, returns {:ok, %{post_id, post_url}} format, constructs post_url from username with /i/ fallback.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add platform-specific caption validation to PostSubmission</name>
  <files>server/lib/clippster_server/social/post_submission.ex</files>
  <action>
Update the private `validate_caption/1` function in PostSubmission to enforce platform-specific character limits.

Current implementation validates all platforms at max 2200 characters. Change to:

```elixir
defp validate_caption(changeset) do
  platform = get_field(changeset, :platform)
  caption = get_field(changeset, :caption) || ""
  hashtag_count = Regex.scan(~r/#\w+/, caption) |> length()

  max_length = case platform do
    "twitter" -> 280
    _ -> 2200
  end

  error_message = case platform do
    "twitter" -> "Twitter captions limited to 280 characters"
    _ -> "caption cannot exceed 2,200 characters"
  end

  changeset
  |> validate_length(:caption, max: max_length, message: error_message)
  |> then(fn cs ->
    if hashtag_count > 30 do
      add_error(cs, :caption, "cannot have more than 30 hashtags")
    else
      cs
    end
  end)
end
```

This preserves the existing hashtag validation while adding platform-aware length checking. Instagram (2200), Twitter (280), all others (2200 default).
  </action>
  <verify>
Run `cd /Users/sn0w/Documents/dev/clippster-mono/server && mix compile --warnings-as-errors` - must compile with zero warnings. Grep for `280` in post_submission.ex to confirm Twitter limit. Grep for `twitter.*280\|280.*twitter` (case-insensitive) to confirm platform-specific validation.
  </verify>
  <done>
PostSubmission.validate_caption enforces 280-char limit for platform "twitter" and 2200-char limit for all other platforms. Clean compilation.
  </done>
</task>

</tasks>

<verification>
1. `mix compile --warnings-as-errors` passes with zero warnings
2. Twitter.create_tweet/3 function exists with correct endpoint and return format
3. PostSubmission validates 280 chars for Twitter, 2200 for Instagram
4. No existing tests broken (run `mix test` if test suite exists)
</verification>

<success_criteria>
- Twitter module has working create_tweet/3 targeting POST https://api.x.com/2/tweets
- create_tweet/3 returns {:ok, %{post_id, post_url}} with username-based URL construction
- PostSubmission enforces 280-char caption limit for Twitter platform
- All code compiles cleanly with --warnings-as-errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-tweet-creation-scheduling/03-01-SUMMARY.md`
</output>
