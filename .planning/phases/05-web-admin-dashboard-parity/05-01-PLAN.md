---
phase: 05-web-admin-dashboard-parity
plan: 01
type: execute
wave: 1
depends_on: ["04-02"]
files_modified:
  - landing/src/main.tsx
  - landing/src/components/AdminRoute.tsx
  - landing/src/components/admin/AdminSidebar.tsx
  - landing/src/layouts/AdminLayout.tsx
  - landing/src/services/adminApi.ts
  - landing/src/pages/admin/*
autonomous: true

must_haves:
  truths:
    - "Website exposes native React admin routes with app-parity path surface (`/admin/*`)."
    - "Admin/moderator access is enforced in the website route layer."
    - "Admin pages are recreated natively in landing React (no iframe/Tauri runtime in browser)."
    - "Deep links are supported under both `/admin/*` and `/dashboard/admin/*`."
  artifacts:
    - path: "landing/src/components/AdminRoute.tsx"
      provides: "Route-level admin/moderator guard for website admin routes"
      exports: ["AdminRoute"]
    - path: "landing/src/main.tsx"
      provides: "Native React admin route map with deep-link support"
      contains: "AdminChildRoutes"
    - path: "landing/src/services/adminApi.ts"
      provides: "Admin API client coverage for all admin route surfaces"
      exports: ["listAdminUsers", "listAdminOrganizations", "listBugReports", "getAnalyticsStats"]
---

<objective>
Implement the admin dashboard directly in the landing React app with the same route surface, page coverage, and backend functionality as the app admin dashboard.
</objective>

<context>
@landing/src/main.tsx
@landing/src/components/AdminRoute.tsx
@landing/src/components/admin/AdminSidebar.tsx
@landing/src/layouts/AdminLayout.tsx
@landing/src/services/adminApi.ts
@landing/src/pages/admin/*
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create native admin route shell with parity path coverage</name>
  <files>
    landing/src/main.tsx
    landing/src/components/AdminRoute.tsx
    landing/src/components/admin/AdminSidebar.tsx
    landing/src/layouts/AdminLayout.tsx
  </files>
  <action>
1. Add an admin route guard for auth + admin/moderator role checks.
2. Register complete admin child routes under `/admin/*`.
3. Mirror deep links under `/dashboard/admin/*` using the same child route set.
4. Ensure admin sidebar navigation works consistently for either base path.
  </action>
  <verify>
1. Non-authenticated users are redirected to `/login`.
2. Non-admin/non-moderator users are redirected to `/dashboard`.
3. Deep links (for example `/admin/users/:id` and `/dashboard/admin/users/:id`) resolve.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Recreate admin pages natively and wire existing backend APIs</name>
  <files>
    landing/src/services/adminApi.ts
    landing/src/pages/admin/*
  </files>
  <action>
1. Implement native React admin pages for users, organizations, reports, analytics, settings, support, staff messaging, affiliates, and logs.
2. Wire each page to existing `/api/admin/*` backend endpoints.
3. Add missing list-level actions for users/organizations (credits, role updates, subscription controls).
4. Preserve existing app admin information architecture and workflows in the web UI.
  </action>
  <verify>
1. `landing` build passes (`yarn build`).
2. Admin pages load data and actions execute against backend APIs.
3. No iframe/postMessage dependency remains for admin routes.
  </verify>
</task>

</tasks>

<success_criteria>
- Website admin is fully native React (no embedded Tauri/Vue app).
- Admin routes and deep links are covered in website routing.
- Admin page functionality matches app backend capabilities.
- Landing build succeeds with all admin pages wired.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-admin-dashboard-parity/05-01-SUMMARY.md`.
</output>
