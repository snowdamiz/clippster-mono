export function getPublicProfileUrl(slug: string): string {
  return `https://clippster.app/clippers/${slug}`;
}

export function getPublicProfilePath(slug: string): string {
  return `clippster.app/clippers/${slug}`;
}
