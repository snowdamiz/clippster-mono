-- Add source_id column to audio tracks to link extracted audio back to its source video
-- ALTER TABLE video_editor_audio_tracks ADD COLUMN source_id TEXT;

-- Add source_id to clip audio tracks as well
-- ALTER TABLE clip_audio_tracks ADD COLUMN source_id TEXT;

