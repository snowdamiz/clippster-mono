{
  "name": "remotion-renderer-sidecar",
  "version": "1.0.0",
  "description": "Node.js sidecar for Remotion video rendering",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && node build.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "remotion": "^4.0.410",
    "@remotion/renderer": "^4.0.410",
    "@remotion/bundler": "^4.0.410"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "esbuild": "^0.19.0"
  }
}
